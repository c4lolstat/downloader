This is a git downloader.

1. Set config:
	In config.js:
		owner: github username
		repo: name of the repository
		targetDir: the folder to copy repo relative to patcher.js
2. run patcher.js - node patcher.js

latestcommit.txt keep track of the latest downloaded commit of the git repository.